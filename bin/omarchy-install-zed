#!/bin/bash

# omarchy:summary=Install Zed Editor and configure it with the current Omarchy theme

set -e

source "$OMARCHY_PATH/install/personal/packaging/zed.sh"
source "$OMARCHY_PATH/install/personal/config/zed.sh"

setsid gtk-launch dev.zed.Zed
