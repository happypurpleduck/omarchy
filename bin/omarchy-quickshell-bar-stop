#!/usr/bin/env bash
# omarchy:summary=Stop the Quickshell omarchy bar
# omarchy:group=quickshell
# omarchy:hidden=true
set -euo pipefail

config_dir="$HOME/.config/quickshell/omarchy-bar"

quickshell kill --path "$config_dir" --any-display >/dev/null 2>&1 || true
